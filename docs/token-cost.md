# 토큰 비용 구조와 절감 장치

파인 6개가 동시에 도는 구조라 토큰 비용은 단일 세션의 6배로 붙는다.
이 문서는 **비용이 실제로 어디서 발생하는지**와 **`setup-team.sh`가 이미 넣어둔
절감 장치가 무엇을 얼마나 줄이는지**를 실측 데이터 기준으로 정리한다.

## 계기: "모델이 바뀌니 bash를 더 쓰는 것 같은데 비용이 늘지 않나"

결론부터: **호출 횟수는 늘지만, 그 증가분의 대부분은 원본이 작은 명령이라
총량 타격이 작다.** 그리고 진짜 비용은 명령 출력이 아니라 입력 쪽에 있다.

## 실측 — rtk 누적 집계

```
Total commands:    2,653
Input tokens:      1.9M
Output tokens:     663.4K
Tokens saved:      1.2M (65.0%)
```

명령별 상위 항목:

| 순위 | 명령                   | 호출 수 | 절약량 | 평균 절약률 |
| ---- | ---------------------- | ------- | ------ | ----------- |
| 1    | `rtk pytest tests/ -q` | 11      | 709.6K | 87.5%       |
| 2    | `rtk grep`             | 671     | 209.8K | 25.8%       |
| 3    | `rtk read`             | 210     | 79.4K  | 14.0%       |
| 4    | `rtk:toml ps aux`      | 6       | 42.6K  | 90.1%       |
| 5    | `rtk ruff check app/`  | 12      | 25.1K  | 85.6%       |

> 재현: `rtk gain`, `rtk gain --history`.
> 위 수치는 2026-08-06 시점 스냅샷이며 누적치라 계속 증가한다.

### 이 표에서 읽어야 할 것

**절약량과 호출 수는 상관이 없다.** `pytest tests/ -q`는 11번 돌고 709.6K를
아꼈고, `grep`은 671번 돌고 209.8K를 아꼈다. 즉 **출력이 큰 소수의 명령**이
절감의 대부분을 만든다. 테스트 러너·린터·`ps aux` 류가 여기 속한다.

**호출이 잦은 명령은 절약률이 낮다.** `grep` 25.8%, `read` 14.0%. rtk가
압축할 여지가 적은 형태라서다. 그런데 이건 뒤집으면 **원본 자체가 작다**는
뜻이기도 하다. bash 호출이 늘어난 부분이 주로 이 저압축 명령이라면,
절약률이 낮아도 총량에 주는 충격은 제한적이다.

## 진짜 비용은 출력이 아니라 입력

`Input 1.9M vs Output 663K` — 입력이 출력의 약 3배다.

Bash 호출 한 번의 비용은 그 명령의 결과 토큰이 아니라, **그 호출로 턴이 하나
늘어나면서 매번 다시 태워지는 시스템 프롬프트 + 누적 대화**다. `rtk grep`이
671번 실행됐다는 건 그만큼 턴이 늘었다는 뜻이고, 파인 6개면 그 구조가 6벌이다.

따라서 절감의 우선순위는 이렇게 된다.

1. **턴당 고정비를 줄인다** (시스템 프롬프트 다이어트) ← 효과 가장 큼
2. **불필요한 턴 자체를 줄인다** (중복 신호 제거)
3. **명령 출력을 압축한다** (rtk) ← 이미 65% 적용 중

## 이미 적용된 절감 장치

### 1. 역할별 스킬 제한 — 턴당 고정비

`setup-team.sh` `[1.6/5]` 섹션.

gstack setup은 스킬 56개를 `~/.claude/skills/`에 전부 깐다. 그 frontmatter는
약 **22.8KB ≈ 5.7K 토큰**이고, 파인이 뜰 때마다 시스템 프롬프트에 들어간다.

- 방치 시: 5.7K 토큰 × 파인 6개 × **매 턴**
- 제한 후: 역할당 3~5개 스킬만 심볼릭 링크

`~/.claude/skills`는 유저 전역이라 파인별로 다르게 만들 수 없다. 그래서
파인마다 `.team/{역할}/.claude/skills`를 만들어 필요한 스킬만 링크하고,
그 디렉터리를 cwd로 claude를 띄우면서 `--setting-sources project`로 전역
스킬을 차단한다. 역할별 허용 목록은 README의 "역할별 스킬 제한" 표 참조.

**이 방식으로 줄지 않는 것**(실측 확인됨):

- claude 빌트인 스킬 약 15개 — 설정 소스와 무관하게 항상 로드된다
- `~/.claude/rules/`, `~/.claude/CLAUDE.md` — cwd가 홈 디렉터리 아래이면
  그대로 로드된다. 파인 cwd는 항상 `$PROJECT_DIR/.team/{역할}`이므로 해당된다

즉 여기서 실제로 줄어드는 것은 **gstack 스킬 frontmatter와 플러그인·에이전트
정의**다. 전역 규칙까지 줄이려면 `~/.claude/rules/`를 홈 밖으로 옮겨야 하는데,
그건 이 리포 밖의 다른 Claude Code 세션에도 영향을 주므로 하지 않는다.

### 2. Stop 훅 중복 신호 가드 — 불필요한 턴

`setup-team.sh` `start_claude_in_pane()`, 마커 파일 `/tmp/team-say/{파인번호}`.

각 파인은 응답이 끝나면 Stop 훅으로 lead에 "응답 종료" 신호를 보낸다. 파인이
보고를 잊어도 harness가 훅을 실행하므로 신호는 반드시 나간다.

문제는 파인이 `say`로 본 보고를 **이미 보낸** 경우다. 그러면 lead는 같은 건에
대해 보고 1회 + 종료 신호 1회, 총 2턴을 태운다. 그래서 `say`가 마커 파일을
남기고, Stop 훅은 마커가 있으면 신호를 생략하며 마커를 소비(삭제)한다.
보고 없이 끝난 응답에서는 마커가 없으므로 신호가 정상 발송된다.

### 3. lead 모델 선택 — 컨텍스트 × 단가

`setup-team.sh` `MEMBER_MODELS` 배열.

lead는 직접 작업하지 않고 배분·수합만 하지만, **모든 보고가 모이므로 컨텍스트가
가장 빨리 불어나는 파인**이다. 비싼 모델 × 최장 컨텍스트 조합을 피해 Sonnet을
쓰고, 깊은 판단이 필요한 architect만 Opus로 둔다.

### 4. rtk 출력 압축

`setup-team.sh` `[1/5]` 섹션이 `rtk init -g --auto-patch`로 훅을 등록한다.
평범한 `git status`·`ls`·`cat`은 harness가 `rtk git status` 형태로 재작성하므로
명령 앞에 `rtk`를 직접 붙일 필요는 없다.

단, `--setting-sources project`가 유저 settings.json(훅)까지 끄기 때문에
`start_claude_in_pane()`이 `PreToolUse`(rtk 재작성 훅)를 `--settings`로 **모든
파인에 명시 주입**한다. lead도 예외가 아니다 — lead는 Stop 훅만 제외된다
(종료 신호의 수신처가 lead 자신이라 무한 루프가 되기 때문).

## 남은 레버와 한계

**습관 하나**: Read/Grep 툴 대신 Bash로 `cat`/`grep`을 쓰는 것. rtk가 재작성해
주긴 하지만 전용 툴 대비 이득이 없다. 다만 현재 구조에서 추가로 짜낼 여지는
크지 않다.

**측정의 한계**: `rtk gain`은 **누적치라 모델 교체 전후를 구분하지 못한다.**
"모델을 바꾸고 나서 실제로 늘었나"를 확인하려면 `rtk gain --history`를 교체
시점 기준으로 잘라 세는 별도 집계가 필요하다. 현재는 구현하지 않았다.

## 참고

- 재현 명령: `rtk gain`, `rtk gain --history`, `rtk discover`
- 관련 코드: `setup-team.sh` `[1/5]`, `[1.6/5]`, `start_claude_in_pane()`
- 관련 문서: README "역할별 스킬 제한", "Stop 훅 — 보고 누락 방지"
